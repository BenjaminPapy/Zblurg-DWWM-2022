<form method="post" action="{{ path('admin_ship_delete', {'id': ship.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ship.id) }}">
    <button class="btn">Delete</button>
</form>
