<form method="post" action="{{ path('admin_size_delete', {'id': size.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ size.id) }}">
    <button class="btn">Delete</button>
</form>
